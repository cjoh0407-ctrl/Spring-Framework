<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.MemberMapper">
	<insert id="insert" useGeneratedKeys="true" keyProperty="mno">
		INSERT INTO tbl_member (name, email, password)
			VALUES ( #{name}, #{email}, #{password} )
	</insert>
	
	<select id="getList" resultType="org.zerock.dto.MemberDTO">
		SELECT * FROM 
			tbl_member
		ORDER BY 
			mno desc
	</select>
	
	<select id="memberById" resultType="org.zerock.dto.MemberDTO">
		SELECT * FROM
			tbl_member
		WHERE
			mno = #{mno}
	</select>
	
	<update id="update">
		UPDATE 
			tbl_member
		SET
			name = #{name}, password = #{password}, email = #{email}, updatedate=now()
		WHERE
			mno = #{mno}
	</update>
	
	<delete id="delete">
		DELETE FROM
			tbl_member
		WHERE
			mno = #{mno}
	</delete>
</mapper>
